# guacamole-server

- Tags: `priority/P1`, `area/product`, `area/control`, `area/refs`, `type/research`, `ref/guacamole-server`
- Upstream: `https://github.com/apache/guacamole-server.git`
- Locked use: `guacd` and gateway-side protocol boundary reference.
- License caution: Apache-2.0 project. Reuse still requires attribution and explicit review.

Guacamole server is useful for compatibility gateway design and keeping gateway responsibilities separate from the LAN native engine.
